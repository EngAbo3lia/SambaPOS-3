<UserControl x:Class="Samba.Presentation.ToastHostControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Samba.Presentation"
             IsHitTestVisible="False">
    <UserControl.Resources>
        <local:ToastTypeToBrushConverter x:Key="ToastTypeToBrushConverter"/>
    </UserControl.Resources>
    <ItemsControl x:Name="ToastList" HorizontalAlignment="Right" VerticalAlignment="Top"
                  Margin="0,12,12,0" IsHitTestVisible="False">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Background="{Binding Type, Converter={StaticResource ToastTypeToBrushConverter}}"
                        CornerRadius="6" Padding="14,10" Margin="0,0,0,8" MaxWidth="380"
                        IsHitTestVisible="False">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.15"/>
                    </Border.Effect>
                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap" FontSize="13"
                               FontWeight="SemiBold" Foreground="#FFFFFF" FontFamily="Segoe UI"/>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>